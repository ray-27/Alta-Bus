[package]
name = "bench"
version = "0.1.0"
edition = "2024"

[dependencies]
protocol = { path = "../protocol" }
publisher-client = { path = "../publisher-client" }
subscriber-client = { path = "../subscriber-client" }